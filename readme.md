Learning react js in 2024.

Virtual DOM, Fibre and reconcilation.


Virtual DOM was actually the copy of real dom (in a tree like structure json object) allows update the component that has changed only.

Fibre is new algorithm to update virtual dom. it provides incremental rendering ,allowing the splitting of rendering work into chunks spread across multiple frames. Good for animations.


More Hooks. useEffect,useRef,useCallback

### useCallback is a React Hook that lets you cache a function definition between re-renders.
